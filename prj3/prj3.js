var names =[];
var counter = 0;
while(names.length < 4)//Keep asking for names until it the needed amount of 4.
{
    names[counter] = prompt("Please enter a name.")
    counter++;// interates every loop
}